#include <stdio.h>

/**
 * main - entry point
 * Description: Prints the name of the file it compiled form, followed by
 * a new line
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
